rabbitmq {
  host = localhost
  port = 5672
  vhost = /
  user = guest
  password = guest
  exchange.name = ""
  exchange.type = direct
}

simulation {
  name        = "RabbitMQ Publishing"
  requests    = 100
  concurrency = 10
  payload     = "{foo:1}"
}
